{
  "routes": [
    {
      "method": "GET",
      "path": "/health-data",
      "handler": "health-data.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/health-data/count",
      "handler": "health-data.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/health-data/find-health",
      "handler": "health-data.findHealth",
      "config": {
        "policies": ["auth"]
      }
    },
    {
      "method": "GET",
      "path": "/health-data/:id",
      "handler": "health-data.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/health-data",
      "handler": "health-data.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/health-data",
      "handler": "health-data.update",
      "config": {
        "policies": ["auth"]
      }
    },
    {
      "method": "DELETE",
      "path": "/health-data/:id",
      "handler": "health-data.delete",
      "config": {
        "policies": []
      }
    }
  ]
}

{
  "routes": [
    {
      "method": "GET",
      "path": "/medicines",
      "handler": "medicines.find",
      "config": {
        "policies": ["auth"]
      }
    },
    {
      "method": "GET",
      "path": "/medicines/count",
      "handler": "medicines.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/medicine",
      "handler": "medicines.findOne",
      "config": {
        "policies": ["auth"]
      }
    },
    {
      "method": "POST",
      "path": "/medicines",
      "handler": "medicines.create",
      "config": {
        "policies": ["auth"]
      }
    },
    {
      "method": "PUT",
      "path": "/medicine",
      "handler": "medicines.update",
      "config": {
        "policies": ["auth"]
      }
    },
    {
      "method": "DELETE",
      "path": "/medicines/:id",
      "handler": "medicines.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
